Never build features because they are common.

Never build dashboards. Dashboards are consumers of data. We don't even have data yet.

Never build analytics. We have no evidence.

Never add AI before deterministic reasoning. We have no validated reasoning.

Never build a recommendation engine. The teacher is the recommendation engine.

Never create unnecessary tables.

Never optimize prematurely.

Never create CRUD without educational purpose.

Never lose educational context.

Prefer educational correctness over software cleverness.
