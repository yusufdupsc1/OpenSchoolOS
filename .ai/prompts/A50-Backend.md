# A50 — Backend

Implement only one vertical slice.

Do not scaffold the whole application.

Every endpoint must support one educational workflow.

Every API must be testable.

Business logic belongs in domain services.

Controllers remain thin.

Never leak persistence into domain.
